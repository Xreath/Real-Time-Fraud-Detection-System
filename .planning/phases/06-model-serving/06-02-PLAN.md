---
phase: 06-model-serving
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - Dockerfile.serving
  - docker-compose.yml
  - .env
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-05

must_haves:
  truths:
    - "fraud-api Docker container builds successfully from Dockerfile.serving"
    - "docker compose up starts fraud-api alongside all existing services"
    - "curl http://localhost:5002/health returns HTTP 200 when container is healthy"
    - "/invocations endpoint accepts JSON transaction payloads and returns fraud scores"
  artifacts:
    - path: "Dockerfile.serving"
      provides: "Docker image for MLflow model serving with LightGBM + sklearn"
      min_lines: 3
      contains: "ghcr.io/mlflow/mlflow:v2.12.2"
    - path: "docker-compose.yml"
      provides: "fraud-api service definition"
      contains: "fraud-api"
    - path: ".env"
      provides: "SERVING_WORKERS and FRAUD_THRESHOLD env vars"
      contains: "SERVING_WORKERS"
  key_links:
    - from: "docker-compose.yml (fraud-api)"
      to: "mlflow service"
      via: "depends_on with service_healthy condition"
      pattern: "depends_on.*mlflow.*service_healthy"
    - from: "docker-compose.yml (fraud-api)"
      to: "Dockerfile.serving"
      via: "build.dockerfile"
      pattern: "dockerfile.*Dockerfile.serving"
    - from: "docker-compose.yml (fraud-api)"
      to: "MinIO"
      via: "MLFLOW_S3_ENDPOINT_URL env var"
      pattern: "MLFLOW_S3_ENDPOINT_URL.*minio"
---

<objective>
Build the Docker serving infrastructure: Dockerfile.serving extending the MLflow base image with inference dependencies, fraud-api service in docker-compose.yml on port 5002, and serving environment variables in .env.

Purpose: Makes the pyfunc model (registered in Plan 01) queryable via REST at http://localhost:5002/invocations with built-in /health endpoint.
Output: Dockerfile.serving, updated docker-compose.yml with fraud-api service, updated .env with SERVING_WORKERS and FRAUD_THRESHOLD.
</objective>

<execution_context>
@/Users/fazlikoc/Desktop/realtime_fraud_detection/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fazlikoc/Desktop/realtime_fraud_detection/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-model-serving/06-01-SUMMARY.md

<interfaces>
<!-- Existing Docker Compose patterns from codebase -->

From docker-compose.yml (mlflow service — pattern to follow):
```yaml
mlflow:
  build:
    context: .
    dockerfile: Dockerfile.mlflow
  container_name: mlflow
  depends_on:
    postgres:
      condition: service_healthy
    minio-init:
      condition: service_completed_successfully
  ports:
    - "5001:5000"
  environment:
    MLFLOW_S3_ENDPOINT_URL: http://minio:9000
    AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-minioadmin}
    AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-minioadmin}
  healthcheck:
    test: curl -f http://localhost:5000/health || exit 1
    interval: 10s
    timeout: 5s
    retries: 5
```

From Dockerfile.mlflow:
```dockerfile
FROM ghcr.io/mlflow/mlflow:v2.12.2
RUN pip install --no-cache-dir psycopg2-binary boto3
```

Port map (avoid conflicts):
- 2181: Zookeeper
- 9092/29092: Kafka
- 8085: Schema Registry
- 8080: Spark Master
- 7077: Spark Master port
- 5432: PostgreSQL
- 9000/9001: MinIO
- 5001: MLflow (host) -> 5000 (container)
- 5002: fraud-api (NEW)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile.serving</name>
  <files>Dockerfile.serving</files>
  <read_first>
    - Dockerfile.mlflow (base image pattern to extend)
  </read_first>
  <action>
Create `Dockerfile.serving` extending the MLflow base image with inference dependencies (per D-05):

```dockerfile
# MLflow Model Serving - Fraud Detection
# LightGBM champion model + preprocessing artifacts via pyfunc
FROM ghcr.io/mlflow/mlflow:v2.12.2

RUN pip install --no-cache-dir \
    psycopg2-binary \
    boto3 \
    lightgbm>=4.0.0 \
    scikit-learn>=1.4.0 \
    pandas>=2.2.0 \
    numpy>=1.26.0
```

Notes:
- psycopg2-binary and boto3 are needed for MLflow to connect to PostgreSQL backend and MinIO artifact store
- lightgbm and scikit-learn are needed by the FraudPyfunc model at inference time
- Do NOT include joblib separately — it is bundled with scikit-learn
- Do NOT add an ENTRYPOINT — the command is specified in docker-compose.yml
  </action>
  <verify>
    <automated>cd /Users/fazlikoc/Desktop/realtime_fraud_detection && test -f Dockerfile.serving && grep -q "ghcr.io/mlflow/mlflow:v2.12.2" Dockerfile.serving && grep -q "lightgbm" Dockerfile.serving && echo "Dockerfile.serving OK"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile.serving exists at project root
    - Dockerfile.serving contains `FROM ghcr.io/mlflow/mlflow:v2.12.2`
    - Dockerfile.serving contains `lightgbm` in pip install
    - Dockerfile.serving contains `scikit-learn` in pip install
    - Dockerfile.serving contains `boto3` in pip install
    - Dockerfile.serving contains `psycopg2-binary` in pip install
    - Dockerfile.serving does NOT contain ENTRYPOINT or CMD
  </acceptance_criteria>
  <done>Dockerfile.serving builds on the MLflow 2.12.2 base image with all inference dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Add fraud-api service to docker-compose.yml and update .env</name>
  <files>docker-compose.yml, .env</files>
  <read_first>
    - docker-compose.yml (existing service patterns, port map, health check conventions)
    - .env (existing env vars)
  </read_first>
  <action>
**docker-compose.yml changes:**

Add a new service section `fraud-api` after the `mlflow` service block (before the `volumes:` section). Follow the exact formatting pattern of the existing mlflow service:

```yaml
  # ===========================================
  # MODEL SERVING (Fraud Detection API)
  # ===========================================

  fraud-api:
    build:
      context: .
      dockerfile: Dockerfile.serving
    container_name: fraud-api
    depends_on:
      mlflow:
        condition: service_healthy
    ports:
      - "5002:5002"
    environment:
      MLFLOW_TRACKING_URI: http://mlflow:5000
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-minioadmin}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-minioadmin}
      FRAUD_THRESHOLD: ${FRAUD_THRESHOLD:-0.5}
      SERVING_WORKERS: ${SERVING_WORKERS:-2}
    command: >
      mlflow models serve
      --model-uri models:/fraud-detection-model@champion
      --host 0.0.0.0
      --port 5002
      --workers ${SERVING_WORKERS:-2}
      --env-manager local
      --no-conda
    healthcheck:
      test: curl -f http://localhost:5002/health || exit 1
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s
    restart: on-failure:3
```

Key configuration details (per decisions):
- Service name: `fraud-api` (per D-08)
- Port: 5002 on host and container (per D-08)
- Model URI: `models:/fraud-detection-model@champion` (per D-04, SERV-04)
- Workers: `${SERVING_WORKERS:-2}` (per D-06)
- `--env-manager local --no-conda` — uses container's pre-installed env, no nested venv (per research)
- `start_period: 60s` — allows model download from MinIO before health checks start (per D-07)
- `retries: 10` with `interval: 15s` — robust health checking during model load
- `restart: on-failure:3` — auto-restart on transient failures (per D-07 hybrid strategy)
- depends_on mlflow with service_healthy — ensures tracking server is up before model download (per D-07)

**.env changes:**

Add the following lines at the end of the file, before any blank trailing lines:

```
# Model Serving (fraud-api)
SERVING_WORKERS=2
FRAUD_THRESHOLD=0.5
```
  </action>
  <verify>
    <automated>cd /Users/fazlikoc/Desktop/realtime_fraud_detection && grep -q "fraud-api" docker-compose.yml && grep -q "SERVING_WORKERS" docker-compose.yml && grep -q "models:/fraud-detection-model@champion" docker-compose.yml && grep -q "SERVING_WORKERS" .env && echo "Compose + env OK"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `fraud-api:` service definition
    - docker-compose.yml fraud-api section contains `dockerfile: Dockerfile.serving`
    - docker-compose.yml fraud-api section contains `"5002:5002"` port mapping
    - docker-compose.yml fraud-api section contains `models:/fraud-detection-model@champion` in command
    - docker-compose.yml fraud-api section contains `MLFLOW_TRACKING_URI: http://mlflow:5000`
    - docker-compose.yml fraud-api section contains `MLFLOW_S3_ENDPOINT_URL: http://minio:9000`
    - docker-compose.yml fraud-api section contains `--env-manager local`
    - docker-compose.yml fraud-api section contains `--no-conda`
    - docker-compose.yml fraud-api section contains `--workers`
    - docker-compose.yml fraud-api section contains `start_period: 60s`
    - docker-compose.yml fraud-api depends_on contains `mlflow` with `condition: service_healthy`
    - docker-compose.yml fraud-api healthcheck test contains `curl -f http://localhost:5002/health`
    - .env contains `SERVING_WORKERS=2`
    - .env contains `FRAUD_THRESHOLD=0.5`
    - No other existing services in docker-compose.yml are modified
  </acceptance_criteria>
  <done>fraud-api service is defined in docker-compose.yml with correct dependencies, port mapping, model URI, health check, and env vars. SERVING_WORKERS and FRAUD_THRESHOLD are in .env.</done>
</task>

</tasks>

<verification>
- `docker compose config --services` includes `fraud-api`
- `docker compose config` parses without errors
- Dockerfile.serving exists with correct base image and dependencies
- .env contains SERVING_WORKERS and FRAUD_THRESHOLD
</verification>

<success_criteria>
- Dockerfile.serving builds on MLflow 2.12.2 with lightgbm + scikit-learn
- fraud-api service in docker-compose.yml on port 5002 with champion alias model URI
- Health check configured with 60s start_period for model download
- SERVING_WORKERS and FRAUD_THRESHOLD in .env with defaults
</success_criteria>

<output>
After completion, create `.planning/phases/06-model-serving/06-02-SUMMARY.md`
</output>
