---
phase: 07-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - Dockerfile.airflow
  - airflow/dags/.gitkeep
  - .env
  - Makefile
autonomous: true
requirements:
  - AFLO-01
  - AFLO-02
  - AFLO-03
  - AFLO-04

must_haves:
  truths:
    - "Airflow webserver is accessible at http://localhost:8081"
    - "Airflow scheduler is running and processing DAGs"
    - "Airflow uses a separate PostgreSQL database (not shared with MLflow)"
    - "DAG files are mounted from airflow/dags/ into the container"
    - "Airflow containers can reach MLflow, MinIO, Kafka, and Spark on the Docker network"
  artifacts:
    - path: "Dockerfile.airflow"
      provides: "Custom Airflow image with required Python packages"
      contains: "apache-airflow"
    - path: "docker-compose.yml"
      provides: "airflow-webserver, airflow-scheduler, airflow-init, postgres-airflow services"
      contains: "airflow-webserver"
    - path: "airflow/dags/.gitkeep"
      provides: "DAG mount directory exists"
    - path: ".env"
      provides: "Airflow environment variables"
      contains: "AIRFLOW_WEBSERVER_PORT"
  key_links:
    - from: "docker-compose.yml (airflow-webserver)"
      to: "postgres-airflow:5433"
      via: "AIRFLOW__DATABASE__SQL_ALCHEMY_CONN"
      pattern: "postgresql.*postgres-airflow"
    - from: "docker-compose.yml (airflow-webserver)"
      to: "mlflow:5000"
      via: "network connectivity"
      pattern: "mlflow"
---

<objective>
Set up Airflow infrastructure in Docker Compose with LocalExecutor, a dedicated PostgreSQL database, and validated connectivity to all existing services.

Purpose: Airflow is the orchestration layer for retraining and monitoring DAGs. Infrastructure must exist before any DAGs can be written.
Output: Dockerfile.airflow, updated docker-compose.yml with 4 Airflow-related services, updated .env, updated Makefile.
</objective>

<execution_context>
@/Users/fazlikoc/Desktop/realtime_fraud_detection/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fazlikoc/Desktop/realtime_fraud_detection/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-orchestration/7-CONTEXT.md

<interfaces>
<!-- Existing docker-compose.yml services and ports in use -->
<!-- Ports in use: 2181 (zookeeper), 5432 (postgres-mlflow), 7077 (spark), 8080 (spark-master UI), 8085 (schema-registry), 9000/9001 (minio), 9092/29092 (kafka), 5001 (mlflow), 5002 (fraud-api) -->

From .env (relevant vars):
```
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=mlflow123
POSTGRES_DB=mlflow
POSTGRES_PORT=5432
AIRFLOW_WEBSERVER_PORT=8081
AIRFLOW__CORE__FERNET_KEY=change-me-in-production
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile.airflow and Airflow Docker Compose services</name>
  <read_first>
    - /Users/fazlikoc/Desktop/realtime_fraud_detection/docker-compose.yml
    - /Users/fazlikoc/Desktop/realtime_fraud_detection/.env
    - /Users/fazlikoc/Desktop/realtime_fraud_detection/Dockerfile.mlflow
  </read_first>
  <files>Dockerfile.airflow, docker-compose.yml, .env, airflow/dags/.gitkeep</files>
  <action>
1. Create `airflow/dags/.gitkeep` (empty file to ensure directory exists in git).

2. Create `Dockerfile.airflow`:
   - Base image: `apache/airflow:2.9.3-python3.11` (stable, compatible with SQLAlchemy 1.4 constraints)
   - Install additional pip packages needed by DAGs: `docker`, `mlflow==2.12.2`, `pandas`, `numpy`, `scikit-learn`, `lightgbm`, `xgboost`, `optuna`, `imbalanced-learn`, `joblib`, `boto3`, `pyspark==3.5.0`, `requests`, `Faker`
   - Use `--constraint` flag pointing to Airflow's constraints file for the version to avoid SQLAlchemy conflicts: `https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.11.txt`
   - Set `USER airflow` at the end

3. Add to `docker-compose.yml` (after the fraud-api section, before volumes):

   **postgres-airflow** service:
   - Image: `postgres:16-alpine`
   - Container name: `postgres-airflow`
   - Environment: `POSTGRES_USER: airflow`, `POSTGRES_PASSWORD: airflow123`, `POSTGRES_DB: airflow`
   - Ports: `5433:5432` (different host port to avoid conflict with MLflow postgres on 5432)
   - Volume: `postgres_airflow_data:/var/lib/postgresql/data`
   - Healthcheck: `pg_isready -U airflow`, interval 5s, timeout 3s, retries 5

   **airflow-init** service (one-shot init container):
   - Build from Dockerfile.airflow
   - Container name: `airflow-init`
   - depends_on: `postgres-airflow: condition: service_healthy`
   - Environment (shared Airflow env block):
     - `AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow123@postgres-airflow:5432/airflow`
     - `AIRFLOW__CORE__EXECUTOR: LocalExecutor`
     - `AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW__CORE__FERNET_KEY:-change-me-in-production}`
     - `AIRFLOW__CORE__LOAD_EXAMPLES: "false"`
     - `AIRFLOW__WEBSERVER__BASE_URL: http://localhost:${AIRFLOW_WEBSERVER_PORT:-8081}`
     - `AIRFLOW__WEBSERVER__WEB_SERVER_PORT: 8081`
     - `MLFLOW_TRACKING_URI: http://mlflow:5000`
     - `MLFLOW_S3_ENDPOINT_URL: http://minio:9000`
     - `AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-minioadmin}`
     - `AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-minioadmin}`
   - entrypoint: `["/bin/bash", "-c"]`
   - command: runs `airflow db init && airflow users create --username admin --password admin --firstname Admin --lastname User --role Admin --email admin@example.com || true`
   - restart: `"no"`

   **airflow-webserver** service:
   - Build from Dockerfile.airflow
   - Container name: `airflow-webserver`
   - depends_on: `airflow-init: condition: service_completed_successfully`, `mlflow: condition: service_healthy`
   - Ports: `${AIRFLOW_WEBSERVER_PORT:-8081}:8081`
   - Same environment block as airflow-init
   - Volumes: `./airflow/dags:/opt/airflow/dags`, `./src:/opt/airflow/src`, `./data:/opt/airflow/data`, `./alerts:/opt/airflow/alerts`, `/var/run/docker.sock:/var/run/docker.sock` (per D-10, D-13)
   - command: `airflow webserver --port 8081`
   - healthcheck: `curl -f http://localhost:8081/health || exit 1`, interval 15s, timeout 10s, retries 10, start_period 30s
   - `user: "${AIRFLOW_UID:-50000}:0"` (needed for Docker socket access on some systems; group 0 = root group for socket read)

   **airflow-scheduler** service:
   - Build from Dockerfile.airflow
   - Container name: `airflow-scheduler`
   - depends_on: `airflow-init: condition: service_completed_successfully`
   - Same environment block as airflow-init
   - Volumes: same as webserver (dags, src, data, alerts, docker socket)
   - command: `airflow scheduler`
   - healthcheck: `airflow jobs check --job-type SchedulerJob --hostname "$(hostname)" || exit 1`, interval 30s, timeout 10s, retries 5, start_period 30s
   - `user: "${AIRFLOW_UID:-50000}:0"`

4. Add `postgres_airflow_data` to the volumes section at bottom of docker-compose.yml.

5. Update `.env` — add if not already present:
   - `AIRFLOW_UID=50000` (for Docker socket permissions)
   - Ensure `AIRFLOW_WEBSERVER_PORT=8081` exists (already present)
   - Ensure `AIRFLOW__CORE__FERNET_KEY=change-me-in-production` exists (already present)

Use YAML anchors (`x-airflow-common`) for shared environment and volume definitions to keep DRY. Define the anchor before the airflow services section.
  </action>
  <verify>
    <automated>cd /Users/fazlikoc/Desktop/realtime_fraud_detection && docker compose config --services 2>&1 | grep -c "airflow" | grep -q "[2-4]" && echo "PASS: Airflow services found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `airflow-webserver` service definition
    - docker-compose.yml contains `airflow-scheduler` service definition
    - docker-compose.yml contains `airflow-init` service definition
    - docker-compose.yml contains `postgres-airflow` service definition
    - docker-compose.yml contains `postgres_airflow_data` in volumes section
    - Dockerfile.airflow contains `FROM apache/airflow`
    - Dockerfile.airflow contains `mlflow==2.12.2`
    - Dockerfile.airflow contains `docker` (Docker Python SDK)
    - Dockerfile.airflow contains `constraints`
    - .env contains `AIRFLOW_UID`
    - airflow/dags/.gitkeep exists
    - postgres-airflow uses port 5433 on host (not 5432)
    - airflow-webserver maps port 8081 (not 8080, avoiding Spark UI conflict)
    - airflow-webserver volumes include `/var/run/docker.sock`
    - airflow-webserver volumes include `./alerts:/opt/airflow/alerts` (per D-13 persistence)
    - airflow-webserver depends_on includes `airflow-init` and `mlflow`
    - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN contains `postgres-airflow` (separate from MLflow postgres)
  </acceptance_criteria>
  <done>Dockerfile.airflow created, docker-compose.yml has 4 Airflow services with separate PostgreSQL, correct port mappings, Docker socket mount, and MLflow connectivity</done>
</task>

<task type="auto">
  <name>Task 2: Add Makefile targets and validate Docker Compose config</name>
  <read_first>
    - /Users/fazlikoc/Desktop/realtime_fraud_detection/Makefile
    - /Users/fazlikoc/Desktop/realtime_fraud_detection/docker-compose.yml
  </read_first>
  <files>Makefile</files>
  <action>
1. Add Airflow section to Makefile after the Application section, before Cleanup:

```makefile
# --- Airflow ---
airflow-up:
	docker compose up -d postgres-airflow airflow-init
	@echo "Waiting for airflow-init to complete..."
	docker compose wait airflow-init 2>/dev/null || sleep 15
	docker compose up -d airflow-webserver airflow-scheduler
	@echo "Airflow starting at http://localhost:$${AIRFLOW_WEBSERVER_PORT:-8081}"

airflow-down:
	docker compose stop airflow-scheduler airflow-webserver
	docker compose rm -f airflow-scheduler airflow-webserver airflow-init

airflow-logs:
	docker compose logs -f airflow-webserver airflow-scheduler --tail=50

airflow-dags:
	docker compose exec airflow-webserver airflow dags list
```

2. Update the `.PHONY` line at top to include: `airflow-up airflow-down airflow-logs airflow-dags`

3. Validate the full docker-compose.yml parses correctly:
   Run `docker compose config --quiet` to verify no YAML syntax errors.
  </action>
  <verify>
    <automated>cd /Users/fazlikoc/Desktop/realtime_fraud_detection && grep -c "airflow" Makefile | grep -q "[4-9]" && docker compose config --quiet 2>&1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains `airflow-up` target
    - Makefile contains `airflow-down` target
    - Makefile contains `airflow-logs` target
    - Makefile contains `airflow-dags` target
    - Makefile .PHONY line includes `airflow-up`
    - `docker compose config --quiet` exits with code 0 (valid YAML)
  </acceptance_criteria>
  <done>Makefile has 4 Airflow targets, docker-compose.yml validates without errors</done>
</task>

</tasks>

<verification>
1. `docker compose config --services` lists airflow-webserver, airflow-scheduler, airflow-init, postgres-airflow
2. `docker compose config --quiet` exits 0 (valid YAML)
3. Dockerfile.airflow exists with apache/airflow base and required packages
4. .env has AIRFLOW_UID variable
5. airflow/dags/ directory exists
6. Makefile has airflow-up, airflow-down, airflow-logs, airflow-dags targets
</verification>

<success_criteria>
- Airflow infrastructure is fully defined in Docker Compose with separate PostgreSQL (AFLO-01, AFLO-03)
- DAG directory mounted into containers (AFLO-04)
- Airflow containers have network access to MLflow, MinIO, Kafka, Spark (AFLO-02)
- Docker socket mounted for container restart capability (D-09, D-10)
- Port 8081 avoids Spark Master UI conflict on 8080
- `docker compose config` validates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-orchestration/07-01-SUMMARY.md`
</output>
